name: Testing

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "*" ]

permissions:
  contents: read

jobs:
  testing:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install uv
      uses: astral-sh/setup-uv@v5
      with:
        version: "0.6.11"
        enable-cache: true

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install the project
      run: uv sync --all-extras
      
    - name: Install plugin
      run: uv pip install pytest-github-actions-annotate-failures

    - name: Collect changed pipeline files
      if: github.event_name == 'pull_request'
      run: |
        CHANGED="$(git diff --name-only "origin/${{ github.base_ref }}...HEAD" -- "brainsets_pipelines/*/pipeline.py")"
        echo "CHANGED_PIPELINES<<EOF" >> "$GITHUB_ENV"
        echo "$CHANGED" >> "$GITHUB_ENV"
        echo "EOF" >> "$GITHUB_ENV"

    - name: Test with pytest
      run: |
        uv run pytest --cov=brainsets --cov-report=xml --cov-report=term-missing -v

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
